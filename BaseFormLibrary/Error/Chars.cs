namespace BaseFormLibrary.Error
{
    public enum Chars
    {
        Any,
        OnlyDigits,
        OnlyEnLetters,
        OnlyRusLetters,
        RusLettersDigits,
        EnLettersDigits
    }
}
